<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Meus Serviços - ContrataFácil</title>
  <link rel="stylesheet" href="/STYLE/style.css" />
  <script src="https://unpkg.com/lucide@latest"></script>
</head>
<body class="home-body">

  <!-- Cabeçalho fixo -->
  <header class="top-bar">
    <div class="logo">🛠️ <span>ContrataFácil</span></div>
    <nav class="menu">
      <a href="/home">Home</a>
      <a href="/logout">Sair</a>
    </nav>
    <div class="perfil">
      <i data-lucide="user"></i>
      <span><%= cliente.nome %></span>
    </div>
  </header>

  <!-- Conteúdo principal -->
  <main class="content meus-servicos">
    <h2>📋 Meus Serviços Contratados</h2>

    <% if (contratos.length === 0) { %>
      <p class="nenhum-servico">Você ainda não contratou nenhum serviço.</p>
    <% } else { %>
      <div class="cards-servicos">
        <% contratos.forEach(c => { %>
          <div class="card-servico">
            <div class="card-info">
              <h3><%= c.profissional_nome %></h3>
              <p class="categoria"><%= c.categoria %></p>
              <p class="detalhe">🗓️ <%= new Date(c.data_contratacao).toLocaleString('pt-BR') %></p>
              <p class="detalhe">📌 Status: <%= c.status %></p>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>

    <a href="/home" class="voltar">← Voltar</a>
  </main>

  <script>lucide.createIcons();</script>
</body>
</html>
