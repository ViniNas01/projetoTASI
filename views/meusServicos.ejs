<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Meus Serviços Contratados</title>
  <link rel="stylesheet" href="/STYLE/style.css" />
  <script src="https://unpkg.com/lucide@latest"></script>
</head>
<body>
  <header class="top-bar">
    <div class="logo">🛠️ <span>ContrataFácil</span></div>
    <div class="perfil">
      <i data-lucide="user"></i>
      <span><%= cliente.nome %></span>
    </div>
  </header>

  <main class="content">
    <h2>📋 Meus Serviços Contratados</h2>

    <% if (contratos.length === 0) { %>
      <p>Você ainda não contratou nenhum serviço.</p>
    <% } else { %>
      <table class="tabela-contratos">
        <thead>
          <tr>
            <th>Profissional</th>
            <th>Categoria</th>
            <th>Data</th>
            <th>Status</th>
          </tr>
        </thead>
        <tbody>
          <% contratos.forEach(c => { %>
            <tr>
              <td><%= c.profissional_nome %></td>
              <td><%= c.categoria %></td>
              <td><%= new Date(c.data_contratacao).toLocaleString('pt-BR') %></td>
              <td><%= c.status %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>

    <a href="/home" class="voltar">← Voltar</a>
  </main>

  <script>lucide.createIcons();</script>
</body>
</html>
