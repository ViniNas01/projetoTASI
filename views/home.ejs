<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Home - ContrataFácil</title>
  <link rel="stylesheet" href="/STYLE/style.css" />
  <script src="https://unpkg.com/lucide@latest"></script>
</head>
<body class="home-body">
  <!-- Cabeçalho -->
  <header class="top-bar">
    <div class="logo">
      🛠️ <span>ContrataFácil</span>
    </div>

    <nav class="menu">
      <a href="/meus-servicos">Meus Serviços</a>
      <a href="/logout">Sair</a>
    </nav>

    <div class="perfil">
      <i data-lucide="user"></i>
      <span><%= cliente.nome %></span>
    </div>
  </header>

  <!-- Conteúdo principal -->
  <main class="content">
    <!-- Barra de pesquisa -->
    <section class="search-section">
      <form action="#" method="GET" class="search-form">
        <input type="text" placeholder="Buscar serviços..." name="q" />
        <button type="submit"><i data-lucide="search"></i></button>
      </form>
    </section>

    <!-- Lista de profissionais -->
    <section class="cards-section">
      <% profissionais.forEach(p => { %>
        <div class="card-profissional">
          <img src="<%= p.imagem %>" alt="<%= p.nome %>" />
          <div class="card-info">
            <h3><%= p.nome %></h3>
            <p class="categoria"><%= p.categoria %></p>
            <p class="detalhe">📍 <%= p.distancia %> de você</p>
            <p class="detalhe">⭐ <%= p.estrelas %> avaliações</p>
            <form action="/profissional/<%= p.id %>" method="GET">
              <button type="submit" class="btn-contratar">Contratar</button>
            </form>
          </div>
        </div>
      <% }) %>
    </section>
  </main>

  <script>lucide.createIcons();</script>
</body>
</html>
